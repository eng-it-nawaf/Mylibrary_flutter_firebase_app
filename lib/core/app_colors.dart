import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF00C9FF);
  static const Color primaryLight = Color(0xFFE3F2FD);

  // Text Colors
  static const Color textPrimary = Color(0xFF00C9FF);
  static const Color textSecondary = Colors.grey;
  static const Color textDark = Color(0xFF333333);

  // Background Colors
  static const Color backgroundLight = Colors.white;
  static const Color backgroundGradientStart = Color(0xFFE3F2FD);
  static const Color backgroundGradientEnd = Colors.white;

  // Indicator Colors
  static const Color activeIndicator = Color(0xFF00C9FF);
  static const Color inactiveIndicator = Color(0xFFE0E0E0);
}